# ── Dependencies & build output ────────────────────────────
node_modules
dist
build
.svelte-kit
.turbo
.cache
coverage
*.tsbuildinfo

# ── Local env (1Password-resolved at load) ────────────────
# .envrc is generated locally and reads secrets from 1Password.
# Never commit it. .envrc.example IS committed as a template.
.envrc
.env
.env.local
.env.*.local

# ── Cloudflare / Wrangler ─────────────────────────────────
.wrangler
.dev.vars
wrangler-cache

# ── Tooling caches ────────────────────────────────────────
.playwright-mcp
.vscode/settings.json
.idea

# ── OS detritus ──────────────────────────────────────────
.DS_Store
Thumbs.db
*.log

# ── Misc ─────────────────────────────────────────────────
*.pem
.tmp
tmp/
